server:
  port: 8003

#feign:
#  hystrix:
#    enabled: true

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:7001/eureka

spring:
  application:
    name: service-feign